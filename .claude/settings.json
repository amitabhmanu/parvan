{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q 'git commit'; then uv run python .claude/hooks/check_provenance.py; fi"
          }
        ]
      }
    ]
  }
}
